# Build reports

Per-run final reports, one file per /ship or multi-task run: YYYY-MM-DD-<taskname>.md.
Written and committed with the build (CLAUDE.md rule). The chat report is ephemeral; these are the record.
